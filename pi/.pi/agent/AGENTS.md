# Global Pi Agent Instructions

## Obsidian Vault

In `pi-sandbox`, my Obsidian vault is mounted at:

- `$PI_OBSIDIAN_VAULT`
- `/home/sandbox/dev/obsidian`

The corresponding host path is `~/dev/obsidian`.

When asked about my Obsidian vault, check these paths rather than only looking in `/workspace`.
